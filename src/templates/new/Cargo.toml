[package]
name = "{{name}}"
version = "0.0.1"
edition = "2021"

[[bin]]
name = "{{name}}"
path = "src/main.rs"

[dependencies]
vynkor-sdk = "0.0.3"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
